# מסלול הבהירות — MVP

## היקף

גרסה זו מפעילה שני מסלולים בלבד:

- `workplan_basic`
- `budget_basic`

המנוע כללי וקורא את התוכן מקובץ JSON. אין חשבונות משתמשים, תשלום, שמירת תשובות, משלוח מייל או אבחוני עומק.

## קבצים חדשים

- `diagnostics/index.html` — בחירת מסלול.
- `diagnostics/assessment.html` — מעטפת השאלון והדוח.
- `diagnostics/engine.js` — מנוע ניקוד טהור, ללא תלות ב־DOM.
- `diagnostics/app.js` — ניהול הזרימה וחוויית המשתמש.
- `diagnostics/diagnostics.css` — עיצוב רספונסיבי והדפסה ל־PDF.
- `data/diagnostics-v1.json` — חבילת נתונים מצומצמת לשני מסלולי ההשקה.
- `tests/diagnostics-engine.test.js` — בדיקות מנוע בסיסיות.

## פרטיות

התשובות נשמרות במשתנה JavaScript בזיכרון העמוד בלבד. אין כתיבה ל־`localStorage`, עוגייה או שרת. רענון או יציאה מוחקים את התשובות. אירועי אנליטיקה, כאשר ניתנה הסכמה קיימת באתר, אינם כוללים תשובות, ציונים או טווח תוצאה; הם כוללים רק התחלה, אבני דרך, השלמה, הפקת דוח והתחלה מחדש.

## הפעלה מקומית

יש להפעיל שרת סטטי משורש האתר, משום ש־`fetch` אינו פועל בצורה אמינה דרך `file://`:

```bash
python -m http.server 8080
```

לאחר מכן לפתוח:

`http://localhost:8080/diagnostics/`

## בדיקות

```bash
node tests/diagnostics-engine.test.js
```

## עדכון נתונים

האקסל הוא מקור העריכה. יש לייצא ממנו JSON מלא, ולאחר מכן לבנות עבור ההשקה קובץ מצומצם הכולל את שני האבחונים, סטי התשובה, ההסתייגויות וקישורי התוכן הנדרשים. אין לשנות מזהים קיימים ללא תהליך migration.

## פער תוכן ידוע

הפעולות באקסל מופעלות רק כאשר ציון קטגוריה הוא `0–39`. לכן תוצאה שבה כל הקטגוריות קיבלו 40 ומעלה אינה מחזירה פעולות מן הנתונים. במצב זה הממשק מציג המלצת שימור כללית וקישור למאמר, ואינו ממציא פעולות חדשות. אם נדרש דוח עם פעולות גם לטווח הביניים, יש להוסיף ל־ACTIONS פעולות בעלות `trigger_min_score=40` ו־`trigger_max_score=69`.
